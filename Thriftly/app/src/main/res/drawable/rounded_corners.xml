<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Bo các góc -->
    <corners android:radius="16dp" />

    <!-- Màu nền -->
    <solid android:color="@android:color/white" />

    <!-- Viền (nếu cần) -->
    <stroke
        android:width="1dp"
        android:color="#DDDDDD" /> <!-- Màu viền tùy chỉnh -->
</shape>
